import 'package:flutter/material.dart';

class DriverTxnScreen extends StatelessWidget {
  const DriverTxnScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
