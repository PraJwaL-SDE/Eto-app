import 'package:flutter/cupertino.dart';

class AllowLocationPopup extends StatelessWidget {
  const AllowLocationPopup({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(

      ),
    );
  }
}
